apiVersion: v1
kind: Service
metadata:
  name: {{ include "jaypiserver.fullname" . }}
  labels:
    {{- include "jaypiserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jaypiserver.selectorLabels" . | nindent 4 }}
