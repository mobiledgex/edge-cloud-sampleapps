apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jaypiserver.fullname" . }}-test-connection"
  labels:
    {{- include "jaypiserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jaypiserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
