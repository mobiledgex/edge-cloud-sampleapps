If updated, these DALI pipelines must be built and placed in their respective `models` directory. The `build.sh` script will do this.

See https://github.com/triton-inference-server/dali_backend for details.